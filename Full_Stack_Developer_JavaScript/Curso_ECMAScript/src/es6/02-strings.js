let hello = 'hello'
let world = 'world'

let epicPhrase = hello +' '+ world + '!'
console.log(epicPhrase)

let epicPhrase2 = `${hello} ${world}! desde template literals
ademas permite agregar otras lineas
 lo que es realmente muy util`
console.log(epicPhrase2)